# Copy to .env.local for local development (never commit secrets).
# Pull managed vars with: npx vercel env pull .env.local

DATABASE_URL=
BLOB_READ_WRITE_TOKEN=
ATLAS_EDIT_SECRET=

# Set after running: npm run db:migrate && npm run db:seed
ATLAS_ID=8f3a2c1e-9b4d-4e6f-a1c2-d3e4f5a6b7c8
ATLAS_SLUG=santa-ruiz
