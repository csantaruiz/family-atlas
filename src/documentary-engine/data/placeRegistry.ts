/** @deprecated Import from canonicalPlaceRegistry.ts — thin re-export for compatibility. */
export {
  allCanonicalPlaces,
  canDriveLocalCamera,
  getCanonicalPlace,
  getRegistryPlace,
  labelVisibleAtScale,
  placesForIds,
  placesForKeys,
  projectCanonicalPlace,
  scaleAtZoom,
  SCALE_ZOOM,
  type CanonicalPlace,
  type PlaceConfidence,
  type ProjectedPlace,
} from './canonicalPlaceRegistry'
